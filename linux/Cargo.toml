[package]
name = "openinkbridge-linux"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
openinkbridge-core = { path = "../core" }

[features]
default = []
remarkable = ["openinkbridge-core/remarkable"]

