{
  "name": "@openinkbridge/web",
  "version": "0.1.3",
  "description": "Unified Web Bridge SDK for E-Ink low-latency stylus drawing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build:artifacts": "node scripts/generate-version.mjs && tsc && esbuild src/global.ts --bundle --format=iife --global-name=OpenInkBridge --outfile=dist/index.global.js",
    "build": "npm run clean && npm run build:artifacts",
    "build:wasm": "npm run clean && node scripts/build-wasm.mjs && npm run build:artifacts",
    "sync:android-sample": "node scripts/sync-android-sample.mjs",
    "check:android-sample": "node scripts/sync-android-sample.mjs --check",
    "test": "npm run build && node --test --test-isolation=none test/web.test.cjs",
    "test:unit": "node --test --test-isolation=none test/web.test.cjs",
    "prepack": "npm run build"
  },
  "keywords": [
    "eink",
    "stylus",
    "remarkable",
    "boox",
    "bigme",
    "low-latency",
    "drawing",
    "canvas"
  ],
  "author": "OpenInkBridge Contributors",
  "license": "Apache-2.0",
  "peerDependencies": {
    "react": "^16.8.0 || ^17.0.0 || ^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "18.3.31",
    "esbuild": "0.28.1",
    "react": "18.3.1",
    "typescript": "5.9.3"
  }
}
